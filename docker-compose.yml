version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    # optionally: environment variables
    # env_file:
    #  - .env.local
